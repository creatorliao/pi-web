# 施工记录：WP-C / D / E

| 日期 | 2026-08-20 |
| 依据 | [11-实施方案](./11-实施方案_20260820_工作空间选择器改版.md) |

- `lib/web-config-store.ts` + `GET/PATCH /api/web-config` → `{agentDir}/web.json`
- `WorkspacePicker` 顶栏动作、小圆角、星标/隐藏
- 左栏文件夹+项目名+「切换」；侧栏 worktree 控件已拆，身份归并仍读 `/api/worktrees`
- L1：`web-config-store` / `project-groups` / sidebar worktree 源码约束 + `tsc --noEmit` 通过
- 未跑 `next build`
- 2026-08-20 晚：点已消失目录的卡片时，`/api/project-trust` 400「Directory does not exist」被 `throw` + `console.error` 打成 Next 红屏。改为选卡先 `cwd/validate`，信任检查失败只收状态、Toast，不再抛。
- F-12：`GET /api/sessions` 标 `directoryExists`；`getRecentProjects` 丢掉已删目录，选择器不再画幽灵卡。不删 jsonl。
