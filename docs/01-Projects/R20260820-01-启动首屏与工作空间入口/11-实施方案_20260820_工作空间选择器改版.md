# 实施方案：工作空间选择器改版（WP-C / D / E）

| 属性 | 值 |
|------|----|
| 日期 | 2026-08-20 |
| 依据 | [09-方案](./09-方案_20260820_工作空间选择器改版.md) Gate-S |
| 状态 | 可施工；与 WP-A/B 闸门状态机并行，不改深链 |

## §1 目标

欢迎页与切换模态：小圆角卡片、顶栏右侧打开目录、星标置顶、隐藏可恢复。左栏是文件夹+项目名。侧栏不出现 worktree。星标/隐藏落 `{getAgentDir()}/web.json`。

## §2 步骤

1. `lib/web-config-store.ts` + 单测：读/补丁/合并未知键/坏 JSON 不覆盖。
2. `GET`/`PATCH /api/web-config`；`arrangeWorkspaceCards`。
3. 改 `WorkspacePicker` 与 `AppShell` 接线（含 Toast）。
4. 左栏入口去下拉脸；拆侧栏 worktree UI；身份归并仍拉 `/api/worktrees`。
5. i18n `en` / `zh-CN`；L1：相关单测 + `tsc --noEmit`。不跑 `next build`。

## §6 验收

| ID | 标准 |
|----|------|
| V-C1 | 打开动作在标题行右；卡片/面板/搜索圆角 4/6/4 |
| V-C2 | 左栏无 ∨，有文件夹标 |
| V-D1 | 星标/隐藏写入 `web.json`，重启仍在 |
| V-D2 | 重置只清 hidden；打开同一目录自动 unhide |
| V-E1 | 左栏无 worktree 下拉/新建/删除/引导 |
| V-F12 | 磁盘上已不存在的目录不出现在欢迎/切换网格；会话 jsonl 仍在 |
| V-F13 | 点失效路径 Toast，无 Next 红屏 |

**不跑 next build。** L3：目视欢迎页与模态。
