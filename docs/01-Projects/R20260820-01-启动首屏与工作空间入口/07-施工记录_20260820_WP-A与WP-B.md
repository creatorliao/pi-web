# 施工记录：WP-A / WP-B

| 属性 | 值 |
|------|----|
| 日期 | 2026-08-20 |
| 分支 | `feat/r01-welcome-gate`（基于 `b16f77e`） |
| 节拍 | ③ 执行 + ④ 验收（用户 batch 授权：施工 / 提交 / 推送 / 升版） |

## 做了什么

- AppShell 相位：`welcome-*` / `deeplink-*` / `ready`；未 ready 不挂载侧栏与三栏
- `WorkspacePicker` 一套网格，欢迎页 + 模态
- 进入不写 `?cwd=`；深链 session 丢失回欢迎；深链 cwd 失败可回欢迎
- 左栏改为项目名 + 开切换器；删除窄下拉与自动选 `projects[0]`
- 删除对话列「开始使用」箭头；`modelsReady` 仅确认 false 时出页脚

## L1/L2

- `tsc --noEmit` 通过
- 新增/回归：`toWorkspaceCards`、`modelsConfigHasModel`、`getInitialNavigation`、自定义目录走 validate 身份
- 未跑 `next build`（仓规：开发期不跑，避免污染 `.next`）
