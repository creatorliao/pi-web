# 施工记录：WP-T1 / WP-T2

| 属性 | 值 |
|------|----|
| 日期 | 2026-08-20 |
| 分支 | 与 R-01 同 `feat/r01-welcome-gate`（T2 与 WP-B 同改 `SessionSidebar`） |
| 节拍 | ③ 执行 + ④ 验收 |

## 做了什么

- 删除 `next/font` Noto；`--font-ui` / 新 `--font-mono`（系统字在前）
- 等宽 ≤11px 升到 12；左栏项目名用 UI 字
- 文件树文件名保持 UI 12；git `+/-` 等宽 12

## L1/L2

- 仓内无 `--font-noto-mono` 残留
- `tsc --noEmit` 通过
