# 工作包 WP-ONLY · 输入框 Cursor 化

| 字段 | 值 |
|------|----|
| 日期 | 2026-08-19 |
| 分支 | 当前工作区直接施工（用户未要求独立 PR） |
| 预计 | 0.5d |

## §1 范围

- **In Scope**：token 解析、高亮层、芯片条、拖高、圆环、N Files、图标化发送/引导、未接线三按钮、i18n、单测。
- **Out of Scope**：语音/审改/撤销真逻辑；新 @ 数据源；contenteditable。

## §3 实施步骤

1. 写 `lib/composer-tokens.ts` + 测试。
2. 写 `ComposerHighlight`、`ContextUsageRing`、写出文件提取。
3. 重组 `ChatInput` 壳层与高度逻辑；`ChatWindow` 传入 usage / files。
4. i18n；`node --test` 相关用例 + tsc。

## §6 验收标准

| V-ID | 描述 | L 层 | 验证方式 |
|------|------|------|----------|
| V-01 | token 解析覆盖多 skill + 多 @ | L1 | composer-tokens 测试 |
| V-02 | ChatInput 原 SSR 断言仍过 | L1 | ChatInput.test.mjs |
| V-03 | typecheck 通过 | L1 | tsc --noEmit |
