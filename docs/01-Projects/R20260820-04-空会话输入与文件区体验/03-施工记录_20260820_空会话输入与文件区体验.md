# 施工记录：空会话输入与文件区体验

| 属性 | 值 |
|------|----|
| 日期 | 2026-08-20 |
| 对照 | [02-方案](./02-方案_20260820_空会话输入与文件区体验.md) |

## 落地

| WP | 文件 | 做了什么 |
|----|------|----------|
| A | `lib/workspace-memory.ts` | `pickWorkspaceSessionToRestore`：记忆优先，否则该工作区 `modified` 最新 |
| A | `components/AppShell.tsx` | `restoreWorkspaceContext` 不再因没有记忆直接 return |
| B | `components/ChatWindow.tsx` + `app/globals.css` | 空态 `chat-empty-composer`：顶对齐 48px，内宽 960 |
| C | `components/FileViewer.tsx` | 原文/预览/差异走 i18n；去掉四路绿点；细栏 A± |
| C | `lib/file-viewer-zoom.ts` | 档位 12–18，写入 `pi-web:file-viewer-zoom` |
| C | `lib/i18n/messages/zh-CN.ts` | 原文 / 预览 / 差异；`fileTextSize` |

## 续：有历史仍空（PR-02 第二层）

| 文件 | 做了什么 |
|------|----------|
| `AppShell.tsx` | 同 cwd 补 key 不再 invalidate；编辑器换项目时辅栏保持开着 |
| `lib/workspace-memory.ts` | `sessionBelongsToWorkspace`：projectKey / root / cwd 都能对上 |

## 续：无文件铺满（2026-08-20 同日）

| 文件 | 做了什么 |
|------|----------|
| `AppShell.tsx` | `editorChatExpanded`：藏空主栏，辅栏 `right-panel-fill` |
| `app/globals.css` | 铺满时辅栏 `flex: 1`，覆盖窄桌面 overlay 固定定位 |

## 续：有对话开历史 · 空态输入钉底（覆盖 WP-B 靠上）

| 文件 | 做了什么 |
|------|----------|
| `AppShell.tsx` | 桌面 `selectedSession?.id` 变化且有会话 → `setAgentHistoryOpen(true)` |
| `ChatWindow.tsx` | 空态：标题 / spacer / 底栏输入，不再靠上粘成一张卡片 |
| `app/globals.css` | `.chat-empty-composer-spacer` 吃掉中间高度；宽度交给 ChatInput 820 |

## 未做（仍 Out of Scope）

文件真编辑、设置页第二套字号、全局 UI 缩放、欢迎页改版。
