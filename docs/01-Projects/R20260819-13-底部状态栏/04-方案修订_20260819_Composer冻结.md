# 方案修订：Composer 冻结（R-13 红线）

| 属性 | 值 |
|------|----|
| 日期 | 2026-08-19 |
| 状态 | 已写入 02-方案 §2.3；后续施工必须遵守 |
| 依据 | 用户：Composer 已完善，解决方案须保证不再改 Composer 任何东西 |
| 前案 | [02-方案](./02-方案_20260819_底部状态栏.md) · [03-压缩进底栏](./03-方案修订_20260819_压缩进底栏.md) |

## 1. 修订一句话

**R-13 余下工作只动窗口壳（底栏左簇、Git、对话分支、Token）。禁止再改现行 Composer。**

D7/D8/D9（喇叭、工具、压缩进底栏）已经落地。那是历史迁出，不是许可证：不可以再回 `ChatInput`「顺手对齐、抽组件、删死 props、改铬条密度」。

## 2. 现行 Composer 冻结面（2026-08-19 对照代码）

框内铬条（`ChatInput` + `composer-chrome` / `composer-tokens`）：

| 仍在 Composer | 不在 Composer（已在 `AppStatusBar`） |
|---------------|--------------------------------------|
| 输入、行内技能高亮、`/` `@`、历史、Steer | 压缩按钮（空闲/进行中/中止） |
| 模型 × 思考成组（portal 向上菜单） | 工具预设四档 |
| 上下文圆环、附件夹子 | 完成提示音喇叭 |
| 发送 ↑ / 停止 ■ 同一圆钮 | |
| 拖高、Bash 条 | |
| 压缩**结果/错误条**（框上方文案，不是按钮） | |
| `/compact` 斜杠命令 | |

以上左列保持原样。右列只许改 `AppStatusBar` / `AppShell` 接线，不许改回 Composer。

## 3. 文件红线

| 禁止改（R-13） | 允许改 |
|----------------|--------|
| `components/ChatInput.tsx` | `components/AppStatusBar.tsx` |
| `lib/composer-chrome.ts` | `components/AppShell.tsx`（只挂底栏，不改传入 `ChatInput` 的铬条 props） |
| `lib/composer-tokens.ts` | `BranchNavigator.tsx` 的 statusbar 变体 |
| 为「干净」去删 ChatInput 上未使用的 `onSoundToggle` / `onToolPresetChange` 类型 | `.app-statusbar*` CSS、statusbar i18n |

`ChatWindow.tsx`：已有 `onComposerStatusChange` 把压缩/工具回调交给壳层。R-13 **不得**再给 `ChatInput` 加减铬条 props，也不得把压缩按钮画回去。

R-01 若另开 Composer 专题，与本夹无关；本夹施工者当 Composer 不存在。

## 4. 作废的旧句（02 正文已改）

- 「本专题从 ChatInput 迁走喇叭与工具」——已完成，不要再施工一遍。
- 「ChatInput.tsx 本夹施工时去掉…」——后续禁止打开该文件。
- 「压缩留在 Composer」与「底栏没有压缩」——以 D9 + 现行代码为准。
